@@ -1,10 +1,10 @@
-module adj_poly_advective_kernel_mod
+module atl_poly_advective_kernel_mod
   use argument_mod, only : arg_type, cell_column, gh_field, gh_inc, gh_read, gh_readwrite, gh_real, gh_write
   use constants_mod, only : i_def, r_def
   use fs_continuity_mod, only : w1, w2, wtheta
   use kernel_mod, only : kernel_type
   implicit none
-  type, public, extends(kernel_type) :: adj_poly_advective_kernel_type
+  type, public, extends(kernel_type) :: atl_poly_advective_kernel_type
   type(ARG_TYPE) :: META_ARGS(5) = (/ &
     arg_type(gh_field, gh_real, gh_readwrite, wtheta), &
     arg_type(gh_field, gh_real, gh_inc, w2), &
@@ -13,15 +13,15 @@
     arg_type(gh_field, gh_real, gh_read, w1)/)
   INTEGER :: OPERATES_ON = cell_column
   CONTAINS
-    PROCEDURE, NOPASS :: adj_poly_advective_code
-END TYPE adj_poly_advective_kernel_type
+    PROCEDURE, NOPASS :: atl_poly_advective_code
+END TYPE atl_poly_advective_kernel_type
 
   private
 
-  public :: adj_poly_advective_code
+  public :: atl_poly_advective_code
 
   contains
-  subroutine adj_poly_advective_code(nlayers, advective, wind, tracer, ls_wind, ls_tracer, ndf_wt, undf_wt, map_wt, ndf_w2, &
+  subroutine atl_poly_advective_code(nlayers, advective, wind, tracer, ls_wind, ls_tracer, ndf_wt, undf_wt, map_wt, ndf_w2, &
 &undf_w2, map_w2, ndf_w1, undf_w1, map_w1)
     integer(kind=i_def), intent(in) :: nlayers
     integer(kind=i_def), intent(in) :: ndf_wt
@@ -133,6 +133,6 @@
     wind(map_w2(3)) = wind(map_w2(3)) + 0.25 * u
     u = 0.0
 
-  end subroutine adj_poly_advective_code
+  end subroutine atl_poly_advective_code
 
-end module adj_poly_advective_kernel_mod
+end module atl_poly_advective_kernel_mod
